
  # Property Inspection App Prototype

  This is a code bundle for Property Inspection App Prototype. The original project is available at https://www.figma.com/design/H1BGocTR8AUB30apm2WJNx/Property-Inspection-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  